# 舊合併版啟動器

這三個 BAT 是 0914 的「模型驗證＋Raw 100 Hz 蒐集」合併流程，已於 0917 從專案根目錄移入封存，避免現場誤開。

目前正式入口：

- `HoloGrip資料蒐集.bat`：只保存 Raw 100 Hz CSV，不載入模型。
- `HoloGrip即時模型驗證.bat`：只做即時七鼓點判斷，不作正式資料蒐集。

兩者都需要獨占左右手 COM，因此一次只開一個。
