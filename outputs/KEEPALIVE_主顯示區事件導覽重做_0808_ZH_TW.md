# KEEPALIVE｜主顯示區與事件導覽重做

## 任務

重做 `HoloGrip_MIDI_CSV對齊檢查_0807_ZH_TW.html` 前端，讓資料設定、三位一體主顯示與事件導覽時間軸分開，並保留既有 MIDI／CSV 對齊與 ±90 ms 窗口分析。

## 已完成

- 三位一體區塊改為主顯示區：影片在左、MIDI＋CSV 局部活動圖在右。
- WAV 音訊改為主顯示區下方的聲音證據列。
- 既有 MIDI 事件播放控制移到主顯示區下方。
- 新增整首 MIDI 事件導覽時間軸，可點擊跳至最近事件。
- 游標、目前事件、前後 ±採樣窗口會同步更新。
- 影片與 WAV 增加 HTML 相對路徑自動載入嘗試，失敗時保留手動選檔回退。

## 驗證

- 4 個 JavaScript 區塊通過 `node --check`。
- `git diff --check` 通過。
- 預設影片、WAV、MIDI、100Hz CSV 實體檔案均存在。
- Browser 工具拒絕直接開啟 `file://` 頁面，因此尚未取得自動化瀏覽器截圖或點擊證據。

## 後續

- 由使用者在本機直接開啟 HTML，確認寬畫面與手機寬度的實際排版。
- 載入影片與 WAV 後，抽查 5:33.085 與 20 ms 密集 MIDI 區。
- 確認無誤後提交 git 進度。
